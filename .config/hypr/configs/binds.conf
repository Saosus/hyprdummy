# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = ALT # Sets "Windows" key as main modifier

#binds, see https://wiki.hypr.land/Configuring/Binds/ for more

#Own scripts
bind = $mainMod, G, exec, ~/.config/hypr/scripts/wallpaper-changer.sh
bind = SUPER, L, exec, hyprctl switchxkblayout at-translated-set-2-keyboard 0 && hyprlock

##WallRizz
#bindd = ,f3, Apply wallpaper preset, exec, $WallRizz
#bindd = $mainMod, f3, Apply random wallpaper, exec, $WallRizzRandom

#Exec
bind = $mainMod, Return, exec, $terminal
bind = $mainMod, P, exec, $fileManager
bind = $mainMod, O, exec, obsidian
bind = $mainMod, I, exec, $terminal -e btop
bind = $mainMod, U, exec, $menu
bind = $mainMod + SHIFT, U, exec, vicinae toggle

bindr = , Print, exec, grim -g "$(slurp -d)" - | wl-copy
bindr = SHIFT, Print, exec, ~/.config/hypr/scripts/taking_screenshots.sh


#window mode
bind = $mainMod, E, togglesplit, # dwindle
bind = $mainMod, T, togglefloating,
bind = $mainMod, W, pseudo, # dwindle
bind = $mainMod, space, exec, hyprctl keyword general:layout "master"

#window size
bind = $mainMod + SHIFT, A, fullscreen, 0
bind = $mainMod, A, fullscreen, 1
binde = $mainMod, R, resizeactive, 35 0
binde = $mainMod + SHIFT, R, resizeactive, -35 0

#kill
bind = $mainMod + SHIFT, code:49, exit,
bind = $mainMod, Q, killactive,

# Move focus/window with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, j, movefocus, u
bind = $mainMod, k, movefocus, d

bind = $mainMod + SHIFT, h, movewindow, l
bind = $mainMod + SHIFT, l, movewindow, r
bind = $mainMod + SHIFT, j, movewindow, u
bind = $mainMod + SHIFT, k, movewindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

bind = $mainMod, F, togglespecialworkspace, worker
bind = $mainMod SHIFT, F, movetoworkspace, special:worker

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
